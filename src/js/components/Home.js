import React from 'react';
import List from './List';
import Form from './Form';

const Home = () => (
    <div>
        <h2>Home</h2>
        <List/>
        <Form/>
    </div>
);

export default Home;